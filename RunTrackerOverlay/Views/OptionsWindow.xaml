<Window x:Class="RunTrackerOverlay.Views.OptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:dt="clr-namespace:RunTrackerOverlay.ViewModels.DesignTime"
        xmlns:converters="clr-namespace:RunTrackerOverlay.Converters"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance dt:DesignTimeOptionsViewModel, IsDesignTimeCreatable=True}"
        Title="Options" Width="300"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="#E0000000"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        KeyDown="OptionsWindow_KeyDown">
    <Window.Resources>
        <converters:TrackerModeToStringConverter x:Key="TrackerModeToStringConverter"/>
    </Window.Resources>
    <Border BorderBrush="Gold" BorderThickness="1">
        <StackPanel Margin="10">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                <TextBlock Text="Session Name: " Foreground="White" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBox Text="{Binding SessionName, UpdateSourceTrigger=PropertyChanged}" Width="150"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                <TextBlock Text="Session Mode: " Foreground="White" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <ComboBox ItemsSource="{Binding AllModes}" SelectedItem="{Binding Mode}" Width="150">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Converter={StaticResource TrackerModeToStringConverter}}"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
            </StackPanel>

            <CheckBox x:Name="SnappingCheckBox" Content="Enable Snapping" Margin="0,0,0,5" IsChecked="{Binding IsSnappingEnabled}" Foreground="White" HorizontalAlignment="Left"/>
            <CheckBox x:Name="ShowSessionNameCheckBox" Content="Show Session Name" Margin="0,0,0,5" IsChecked="{Binding ShowSessionName}" Foreground="White" HorizontalAlignment="Left"/>
            <CheckBox x:Name="ShowRunCountCheckBox" Content="Show Run Count" Margin="0,0,0,5" IsChecked="{Binding ShowRunCount}" Foreground="White" HorizontalAlignment="Left"/>
            
            <CheckBox x:Name="TooltipCheckBox" Content="Show Keys Tooltip" Margin="0,0,0,5" IsChecked="{Binding ShowKeysTooltip}" Foreground="White" HorizontalAlignment="Left"/>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                <TextBlock Text="Timer Format: " Foreground="White" VerticalAlignment="Center"/>
                <TextBox Text="{Binding TimerFormat, UpdateSourceTrigger=PropertyChanged}" Width="150" Margin="5,0,0,0">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsTimerFormatInvalid}" Value="True">
                                    <Setter Property="Background" Value="Salmon"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>
            </StackPanel>
            <CheckBox x:Name="ApplyFormatToStatsCheckBox" Content="Apply format to stats timers" Margin="0,0,0,5" IsChecked="{Binding ApplyFormatToStats}" Foreground="White" HorizontalAlignment="Left"/>

            <TextBlock Text="Show Stats:" Foreground="Gold" Margin="0,5,0,5" FontWeight="Bold"/>
            <Grid Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <CheckBox Grid.Row="0" Grid.Column="0" Content="Best" IsChecked="{Binding ShowBest}" Foreground="White" Margin="0,0,0,5"/>
                <CheckBox Grid.Row="0" Grid.Column="1" Content="Last" IsChecked="{Binding ShowLast}" Foreground="White" Margin="0,0,0,5"/>
                <CheckBox Grid.Row="0" Grid.Column="2" Content="Worst" IsChecked="{Binding ShowWorst}" Foreground="White" Margin="0,0,0,5"/>
                <CheckBox Grid.Row="1" Grid.Column="0" Content="Avg" IsChecked="{Binding ShowAvg}" Foreground="White" Margin="0,0,0,5"/>
                <CheckBox Grid.Row="1" Grid.Column="1" Content="Total" IsChecked="{Binding ShowTotal}" Foreground="White" Margin="0,0,0,5"/>
                <CheckBox Grid.Row="1" Grid.Column="2" Content="Loot" IsChecked="{Binding ShowLoot}" Foreground="White" Margin="0,0,0,5"/>
            </Grid>
            
            <Grid Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Start/Stop/Next Run: " VerticalAlignment="Center" Foreground="White"/>
                <Button Grid.Row="0" Grid.Column="1" x:Name="KeySelectionButton" Content="{Binding StartStopNextKeyText}" Margin="10,2" Click="KeySelectionButton_Click"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Stop (Cont.): " VerticalAlignment="Center" Foreground="White"/>
                <Button Grid.Row="1" Grid.Column="1" x:Name="StopContKeySelectionButton" Content="{Binding StopContKeyText}" Margin="10,2" Click="StopContKeySelectionButton_Click"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Pause/Resume: " VerticalAlignment="Center" Foreground="White"/>
                <Button Grid.Row="2" Grid.Column="1" x:Name="PauseResumeKeySelectionButton" Content="{Binding PauseResumeKeyText}" Margin="10,2" Click="PauseResumeKeySelectionButton_Click"/>

                <TextBlock Grid.Row="3" Grid.Column="0" Text="Focus: " VerticalAlignment="Center" Foreground="White"/>
                <Button Grid.Row="3" Grid.Column="1" x:Name="FocusKeySelectionButton" Content="{Binding FocusKeyText}" Margin="10,2" Click="FocusKeySelectionButton_Click"/>

                <TextBlock Grid.Row="4" Grid.Column="0" Text="Record Loot: " VerticalAlignment="Center" Foreground="White"/>
                <Button Grid.Row="4" Grid.Column="1" x:Name="LootKeySelectionButton" Content="{Binding LootKeyText}" Margin="10,2" Click="LootKeySelectionButton_Click"/>

                <TextBlock Grid.Row="5" Grid.Column="0" Text="Background Opacity: " VerticalAlignment="Center" Foreground="White"/>
                <Slider Grid.Row="5" Grid.Column="1" x:Name="OpacitySlider" Margin="10,2" Minimum="0.0" Maximum="1.0" TickFrequency="0.1" IsSnapToTickEnabled="True" Value="{Binding WindowOpacity}"/>

                <TextBlock Grid.Row="6" Grid.Column="0" Text="Text Opacity: " VerticalAlignment="Center" Foreground="White"/>
                <Slider Grid.Row="6" Grid.Column="1" x:Name="TextOpacitySlider" Margin="10,2" Minimum="0.1" Maximum="1.0" TickFrequency="0.1" IsSnapToTickEnabled="True" Value="{Binding TextOpacity}"/>
            </Grid>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="1" Content="Cancel" Margin="5,0,0,0" IsCancel="True" Click="CancelButton_Click"/>
                <Button Grid.Column="0" Content="Save" Margin="0,0,5,0" IsDefault="True" Click="SaveButton_Click"/>
            </Grid>
        </StackPanel>
    </Border>
</Window>
